<p id="notice"><%= notice %></p>

<h1>Events</h1>

<table>
  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= link_to event.denominacao, event_artists_path(event) %></td>
        <td><%= event.realizacao %></td>
      </tr>
    <% end %>
  </tbody>
</table>
